/**
 * @description **最长回文子串**
 *
 * 给你一个字符串 s，找到 s 中最长的回文子串。
 *
 * 如果字符串的反序与原始字符串相同，则该字符串称为回文字符串。
 *
 * 提示：
 *
 * 1 <= s.length <= 1000
 * s 仅由数字和英文字母组成
 * @param s
 * @inheritDoc https://leetcode.cn/leetbook/read/array-and-string/conm7/
 */
export default function longestPalindrome(s: string): string {
  return s;
}
